<div class="contents row">
  <p><%= @nickname %>さんの投稿一覧</p>
  <% @catches.each do |catch| %>
    <div class="content_post" style="background-image: url(<%= catch.image %>);">
      <p><%= catch.content %></p>
      <span class="title"><%= catch.title %></span>
    </div>
  <% end %>
</div>